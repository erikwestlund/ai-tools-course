# Practice: Simulated-Data Privacy Workflow

This practice task matches the module skeleton so the class can follow along at the same time.

Use only `sim-data/` with AI tools. Treat `real-data/` as a restricted environment, even though the files are simulated for class.

Prompt files are in `practice/prompts/03_simulated-data-privacy-workflow/`.
