This is metadata from a real dataset. Use the metadata in `real-data-metadata/` to generate a simulated dataset so we can use AI tools to work on it.

Write the simulated dataset to:

```text
data/nhanes_2021_2023_analysis.csv
```
