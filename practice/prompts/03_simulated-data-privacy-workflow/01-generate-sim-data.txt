Work only in `practice/work/03_simulated-data-privacy-workflow/sim-data/`.

Do not inspect or use files in `../real-data/`.

The `metadata/` directory contains approved outputs from the real-data side. Complete `generate-sim-data.R` so it reads the metadata and writes:

```text
data/nhanes_2021_2023_analysis.csv
```

The simulated file must use the same file name, column names, broad data types, categorical values, and plausible numeric ranges described by the metadata.

Create simulated IDs. Do not recreate real IDs or linkages.
